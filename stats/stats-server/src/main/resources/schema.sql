drop table IF EXISTS stats CASCADE;

CREATE TABLE stats (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, app VARCHAR(255) NOT NULL, uri VARCHAR(255) NOT NULL, ip VARCHAR(255) NOT NULL, datetime VARCHAR(255) NOT NULL, CONSTRAINT pk_stats PRIMARY KEY (id));